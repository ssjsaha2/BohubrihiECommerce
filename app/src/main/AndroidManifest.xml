<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bohubrihiecommerce">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:allowClearUserData="true"
        android:label="@string/app_name"
        android:name=".utils.BohubrihiECommerceApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BohubrihiECommerce">
        <activity
            android:name=".activities.TourActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SignupOrLoginActivity"/>
        <activity android:name=".activities.ProductActivity"/>
        <activity android:name=".activities.HomeActivity"/>
    </application>

</manifest>